{%- assign first = true -%}
require "spec"
require "../src/*"

describe "{{- json.exercise_cammel -}}" do
{% for cases in  json.cases %}
    {% include "./bin/extra-templates/generator_help", first: first %} "{{ cases.description }}" do
        {{ json.exercise_cammel }}.{{ cases.property | underscore }}("{{ cases.input.phrase }}").should eq("{{ cases.expected }}")
    end
{% endfor %}
end
