require "spec"
require "../src/*"

describe "<%-= to_captlized(@json["exercise"].to_s) %>" do
<%- @json["cases"].as_a.each do |cases| %>
    <%= status()%> "<%-= cases["description"] %>" do
    <%- if cases["expected"].as_h? %>
    expect_raises(ArgumentError) do
        <%= to_captlized(@json["exercise"].to_s) %>.<%= to_snake(cases["property"].to_s)%>(<%= cases["input"]["number"] %>)
    end
    <%- else %>
    <%= to_captlized(@json["exercise"].to_s) %>.<%= to_snake(cases["property"].to_s)%>(<%= cases["input"]["number"] %>).should eq(<%= cases["expected"]%>)
    <%- end %>
    end
<% end %>
end