{%- assign first = true -%}
require "spec"
require "../src/*"

describe "{{- json.exercise_cammel -}}" do
{% for cases in  json.cases %}
    {% include "./bin/extra-templates/generator_help", first: first %} "{{ cases.description }}" do
        {{ json.exercise_cammel }}.find("{{ cases.input.subject }}", {{ cases.input.candidates }}).should eq({{ cases.expected }} of String)
    end
{% endfor %}
end
