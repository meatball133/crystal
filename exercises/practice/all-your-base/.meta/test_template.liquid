{%- assign first = true -%}
require "spec"
require "../src/*"

describe "{{- json.exercise_cammel -}}" do
{% for cases in json.cases %}
    {% include "./bin/extra-templates/generator_help", first: first %} "{{ cases.description }}" do
        input_base = {{ cases.input.inputbase }}
        digits = {{ cases.input.digits }} of Int32
        output_base = {{ cases.input.outputbase }}
        {{ json.exercise_cammel }}.{{ cases.property | underscore }}(input_base, digits, output_base).should eq({{ cases.expected }})
    end
{% endfor %}
end
